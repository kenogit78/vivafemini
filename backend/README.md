# VivaFemini — Backend

NestJS REST API with MongoDB. See the [root README](../README.md) for full setup, seeding, and environment variables.

```bash
npm install
cp .env.example .env
npm run start:dev
```

API: http://localhost:3001/api  
Swagger: http://localhost:3001/api/docs
