# VivaFemini — Frontend

Next.js 14 app. See the [root README](../README.md) for full setup, seeding, and environment variables.

```bash
npm install
cp .env.example .env.local
npm run dev
```

Runs at http://localhost:3000
